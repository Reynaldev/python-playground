import caesar 
import playfair

if __name__ == "__main__": 
    # encoded = caesar.encode("Hello, World! zzz...")
    # decoded = caesar.decode(encoded)
    #
    # print(f"{encoded}\n{decoded}")

    print(playfair.encode("hhelloe"))
